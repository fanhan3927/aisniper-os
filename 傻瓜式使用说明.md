# AISniper OS 使用说明（傻瓜版）

> 这是一套**在浏览器里运行的假桌面系统**——不是真的操作系统，不会动你电脑上的任何文件。所有网络、CPU、文件都是「演出来的」模拟数据，随便点、随便玩，坏了刷新页面就行。

---

## 一、怎么打开它

1. 在浏览器（推荐 **Chrome** 或 **Safari**）地址栏输入：`http://localhost:5173`
2. 按一下键盘的 **F11** 全屏，效果最好
3. 第一次打开会有一段 1.5 秒的开机动画（星核亮起），**点一下屏幕可跳过**
4. 之后刷新页面不会再播动画（只有第一次播）

> 手机也能玩：直接打开网址即可，窗口会自动铺满屏幕，游戏里有虚拟摇杆。

---

## 二、桌面长什么样（认识界面）

从上到下：

| 位置 | 是什么 | 干嘛用 |
|---|---|---|
| 顶部一整条 | 菜单栏 | 左侧是 Logo 和当前 App 名字；右侧是 Wi-Fi、健康、电池图标和时钟 |
| 中间大片 | 桌面 | 点空白处可以关掉所有弹窗；左上角有两个桌面图标（Finder、太空射击） |
| 底部一排玻璃槽 | Dock（快捷栏） | 五个 App 的启动按钮，鼠标放上去会放大 |

---

## 三、顶部菜单栏怎么用

### 1. 点右侧的**时钟**
- 弹出日历：今天有蓝色圆点，可以点 ‹ › 翻月
- 日历下面是三个城市的时间（北京 / 东京 / 纽约）

### 2. 点 Wi-Fi 图标（像信号的扇子）
- 弹出面板：开关、当前网络「AISniper-Net」、附近网络列表
- 把开关关掉 → 右上角会弹出提示「Wi-Fi 已关闭」
- 点列表里的任意网络，会「连上」并弹提示（都是假的）

### 3. 点健康图标（像心电图）
- 弹出面板：CPU、内存、GPU、温度、风扇转速，右上角是健康总分（绿 ≥80 / 黄 50–79 / 红 <50）
- 下面有最近 30 秒的趋势折线
- 玩太空射击游戏时，CPU/GPU 会「变高」——这是故意的

> 三个弹窗**同时只能开一个**：点另一个会自动切换。按 **Esc** 或点桌面空白处即可关闭。

---

## 四、窗口怎么操作（所有 App 通用）

每个 App 打开都是一个窗口，顶部有一排标题栏：

- **拖动标题栏** → 移动窗口
- **双击标题栏** → 最大化 / 还原
- **右下角小斜线** → 按住拖动缩放窗口
- **左上角三个圆点（红黄绿）**
  - 🔴 红：关闭窗口
  - 🟡 黄：最小化（收进 Dock，点 Dock 图标还原）
  - 🟢 绿：最大化（游戏窗口的绿点是**全屏**）
- **点窗口任意处** → 把窗口带到最前面

---

## 五、底部 Dock（快捷栏）

底部五个图标，鼠标放上去会放大、旁边的图标会让位：

1. **Finder** — 文件浏览器
2. **计算器** — 计算器
3. **设置** — 系统设置（换主题就在这）
4. **终端** — 命令行
5. **太空射击** — 3D 射击游戏

规则：
- 图标下方有**小圆点** = 这个 App 正开着；圆点是强调色的 = 当前在前台
- 点已开着的图标 → 把它调到前面（或从最小化还原）
- 点没开的图标 → 打开它

---

## 六、五个 App 逐个教

### 📁 Finder（文件浏览器）
1. 点 Dock 第一个图标打开
2. 左边是分区：个人 / 桌面 / 文稿 / 下载 / 应用 / 太空档案 / 废纸篓
3. **双击文件夹**进入下一层；工具栏 ‹ › 是后退/前进
4. 右上角可切换「图标视图 / 列表视图」，旁边是搜索框（输入即过滤）
5. 常用操作：
   - **＋ 新建文件夹**：建一个文件夹，马上能改名
   - **双击文件名** → 改名（回车确认，Esc 取消）
   - **点选文件 → 点垃圾桶图标** → 移入废纸篓；进废纸篓后可「清空」
   - **双击文本文件** → 弹出 Quick Look 预览
   - **双击「太空射击.app」** → 直接启动游戏！
6. 窗口标题会跟着你进的文件夹变

### 🧮 计算器
1. 打开后直接用鼠标点按钮，或者**用键盘数字键**（+ - * / 回车 = 都支持，Esc 清空）
2. 点左下角「**科学**」→ 窗口变宽，出现 sin/cos/tan/log/ln/√/π/e 等
3. 输入 `1 + 2 回车` → 显示 3（浮点误差已处理，0.1+0.2 = 0.3）

### ⚙️ 设置（换主题的关键入口）
左边选分类，右边操作，**改完立刻全局生效**：

- **外观**：
  - 浅色 / 深色 / 自动（自动=跟随你电脑的深浅色）
  - **Clear ↔ Tinted 滑杆**：往左更通透，往右更实、文字更好认
  - **强调色**：蓝/紫/青/橙/玫瑰，点圆点即换（开关、Dock 指示点都会变）
  - **图标外观**：深色/浅色/着色/Clear，下面有实时预览
- **桌面与 Dock**：
  - **壁纸**：深空（默认）/ 极光 / 日出湖面 / 纯色，点卡片即换
  - Dock 放大开关、减少动态效果开关
- **时钟**：12/24 小时、中文/English
- **声音**：界面音效开关
- **关于本机**：版本号、模拟芯片「Sniper Neural GPU」

### 💻 终端（命令行）
1. 打开后输入命令按回车。提示符是 `sniper@aisniper ~ %`
2. 先输入 `help` 看看有哪些命令；按 ↑↓ 方向键可以翻历史
3. 常用命令（全部可试）：

| 命令 | 效果 |
|---|---|
| `help` | 列出所有命令 |
| `clear` | 清屏 |
| `date` | 显示当前时间 |
| `whoami` | 我是谁（sniper） |
| `neofetch` | 系统信息展示 |
| `ls` / `cat 文件名` | 看文件 / 看文件内容 |
| `open game` | 打开太空射击游戏 |
| `play` | 同上，开始游戏 |
| `theme dark` / `theme light` | 切深色/浅色主题 |
| `health` | 看健康度 |
| `wifi on` / `wifi off` | 开/关 Wi-Fi |
| `echo 任意文字` | 回显 |
| `uname` | 内核信息 |

> 只认上面这些命令，别的会提示 `command not found`——这是故意的，安全第一。

### 🚀 太空射击（3D 游戏）
1. 从 Dock 点图标打开（或终端输入 `play`，或 Finder 双击「太空射击.app」）
2. 点「**开始游戏**」

**操作方式：**
- 桌面电脑：鼠标移动飞船 / 或 WASD / 方向键；**空格或按住鼠标左键**射击
- 手机：左下角**虚拟摇杆**移动，右下角 **✦ 按钮**射击

**界面：**
- 左上角：得分、连击（连杀 5 个获得 ×2 倍率）、右上角生命星星
- 右上角：当前波次、「⏸ 暂停」「⤢ 全屏」
- 右下角：雷达小地图（红点=敌机，黄点=陨石）

**规则：**
- 敌机一波波来，每 3 波出现一艘大巡洋舰（血厚分高）；还有陨石
- 被撞到掉一条命，共 3 条；命用完游戏结束
- 结束后**自动写入本地排行榜 Top 10**，可以「再玩一次」

**全屏：**
- 点右上角「⤢」或点窗口绿色交通灯 → 游戏铺满全屏，菜单栏和 Dock 自动藏起来
- 鼠标移到屏幕最底部 → Dock 会滑出来
- 按 **Esc** 或 **P** 暂停

---

## 七、快捷键速查表

| 按键 | 作用 |
|---|---|
| `Esc` | 关闭弹窗 / 暂停游戏 |
| `P` | 暂停 / 继续游戏 |
| `Enter`（游戏主菜单） | 开始游戏 |
| `⌘/Ctrl + W` | 关闭当前最前面的窗口 |
| `F11` | 浏览器全屏（体验最佳） |

---

## 八、常见问题

**Q：窗口拖不动 / 按钮点不到？**
刷新页面即可（所有状态都是内存里的）。

**Q：为什么我的主题改了没反应？**
确认点的是设置 → 外观里的「浅色/深色」；如果还是不行，刷新一下。

**Q：游戏很卡？**
设置会自动检测低配置降低画质；也可以把浏览器窗口缩小，或退出其他标签页。

**Q：会弄坏我的电脑吗？**
不会。100% 纯前端模拟，不读你磁盘、不连你网络、没有真实权限。

**Q：数据会保存吗？**
会：主题设置、游戏排行榜、开机动画标记会存在浏览器本地（localStorage）。其余（文件、Wi-Fi、健康度）刷新即重置。

---

## 九、一句话总结

**点开网址 → F11 全屏 → 点 Dock 里的图标 → 玩。**
先试设置里换主题换壁纸，再进游戏打一局，这套「假系统」就算体验完了。
