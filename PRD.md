# AISniper OS PRD

浏览器内「下一代桌面操作系统」体验。视觉与交互对标 **macOS Tahoe 26 / macOS Golden Gate 27** 的 **Liquid Glass** 设计语言：通透菜单栏、多层玻璃 Dock、镜面高光、折射与主题色自适应。产品定位不是仿制 macOS，而是把「玻璃材质 + 空间层次 + 桌面隐喻」推进到可在浏览器里直接运行的 **AISniper OS**。

## 产品一句话

在浏览器全屏窗口中运行一套有「看头」的操作系统壳：时钟、状态弹窗、主题切换、底部快捷栏，以及内置可交互的 3D 太空射击游戏。

## 目标用户

- 想做作品集 / Demo Day 的前端与创意工程师
- 喜欢「假 OS / WebOS」实验的设计师
- 需要一个可展示 3D + UI 系统整合能力的演示项目

## 核心功能

1. **桌面舞台（Desktop Stage）**
   - 全视口桌面，默认动态深空壁纸（缓慢漂移星云 / 星点视差），可被主题切换替换
   - 顶部 **透明菜单栏**（Liquid Glass Menu Bar）：左侧为 Apple 风格 Logo 区改为 **AISniper** 字标 + 当前前台 App 名；右侧为状态簇（Wi-Fi、系统健康、电池模拟、时钟）
   - 点击桌面空白处关闭所有弹出层；窗口可重叠，前台窗口有更高的玻璃边与阴影
   - 首次进入播放 1.2s 开机动画：黑色 → 星核亮起 → 桌面淡入（可跳过，只播一次，记入 localStorage）

2. **时钟（Menu Clock）**
   - 菜单栏最右侧实时时钟，格式随区域：默认 `周二 8月26日 01:15` 或 `Tue Aug 26  1:15 AM`（设置里可切换 12/24 小时与中/英）
   - 点击时钟弹出 **日历 + 世界时** 小面板：本月日历高亮今天，下方列出 3 个城市时间（可配置）
   - 数字使用可变字重的等宽数字（仿 SF 数字嵌套），秒针不显示在菜单栏以免跳动干扰

3. **系统状态弹窗（Control / Status Popovers）**
   - **Wi-Fi 弹窗**：点击菜单栏 Wi-Fi 图标弹出。包含开关、当前网络名（模拟 `AISniper-Net`）、信号强度动画、附近网络列表（静态 mock + 选中高亮）、隔空投送式「发现设备」占位。关闭 Wi-Fi 后桌面右上角短暂 toast「未连接」
   - **系统健康度弹窗**：点击心跳 / 活动监视器风格图标。展示 CPU、内存、GPU（游戏运行时升高）、温度、风扇（全部为平滑模拟曲线，不是真系统数据）。健康总分 0–100，颜色：绿 ≥80 / 黄 50–79 / 红 <50。附 30 秒迷你折线图
   - 弹窗均为 **Liquid Glass 卡片**：圆角 20–24px、背景模糊、边缘 1px 半透明描边、顶部轻微镜面高光。打开/关闭用 spring，不得生硬 fade
   - 同时只允许一个状态弹窗打开；再点另一个会切换

4. **底部快捷栏（Dock）**
   - 屏幕底部居中悬浮 Dock，多层玻璃材质，图标为 squircle（超椭圆）+ 轻微 3D 层叠高光
   - **必须包含且仅默认固定这 5 个入口**（可在设置里调整顺序，不可删除）：
     1. **Finder** — 文件浏览器窗口
     2. **计算器** — 玻璃计算器
     3. **设置** — 外观 / 主题 / 时钟 / 壁纸 / 辅助功能
     4. **命令行终端** — 可交互的伪 shell
     5. **太空射击** — 内置 3D 游戏
   - Hover：图标放大 + 邻近图标轻微让位（macOS Magnification，强度可在设置关闭）
   - 打开中的 App 在图标下方显示小圆点指示器；最小化窗口点击 Dock 图标还原
   - Dock 不遮挡游戏全屏；游戏全屏时 Dock 自动上浮隐藏，鼠标移到底部 12px 再滑出

5. **内置 App：Finder**
   - 窗口：侧边栏（个人、桌面、文稿、下载、应用、太空档案）+ 主列表/图标视图切换
   - 预置若干虚拟文件与文件夹（README、壁纸、游戏回放占位、系统日志），支持重命名、新建文件夹、移入废纸篓（内存态，刷新可重置）
   - 双击「太空射击.app」打开游戏；双击图片用快速查看（Quick Look）遮罩预览
   - 顶部工具栏：前进后退、路径、搜索框（本地过滤）

6. **内置 App：计算器**
   - 标准四则运算 + 百分比 + 正负号 + 小数，键盘可用
   - 显示屏大号等宽数字；按键为玻璃圆形/圆角矩形，按下有凹陷高光
   - 可选科学模式（展开后窗口变宽：sin/cos/tan/log/ln/√/π/e/^）

7. **内置 App：设置**
   - 侧边分类：外观、桌面与 Dock、时钟、声音（可静音 UI）、关于本机
   - **主题切换**（核心）：
     - 外观：浅色 / 深色 / 自动（跟随 `prefers-color-scheme`）
     - Liquid Glass 通透滑杆：Clear ↔ Tinted（影响所有玻璃层的模糊与不透明度）
     - 强调色：多色点选（蓝、紫、青、橙、玫瑰），影响开关、焦点环、Dock 指示点
     - 图标外观：深色 / 浅色 / 着色 / 通透 Clear
     - 壁纸：深空默认、极光、日出湖面、纯色、自定义 URL（cors 友好图）
   - 关于本机：AISniper OS 版本号、模拟芯片「Sniper Neural GPU」、内存占用展示

8. **内置 App：命令行终端**
   - 深色等宽终端窗口，提示符 `sniper@aisniper ~ %`
   - 支持命令：`help`、`clear`、`date`、`whoami`、`neofetch`（ASCII + 系统信息）、`ls`、`cat`、`open <app>`（open finder/calc/settings/game）、`theme dark|light`、`health`、`wifi`、`play`、`echo`、`uname`
   - 命令历史上/下箭头；不可执行任意 JS（安全：白名单命令）
   - 启动时一行欢迎语：「Welcome to AISniper OS — targeting the next desktop.」

9. **内置 App：3D 太空射击游戏（AISniper）**
   - 窗口模式与全屏模式；从 Dock / Finder / 终端 `play` 进入
   - 玩法：第三人称或固定视角的太空通道射击。玩家飞船在屏幕下部，鼠标/方向键移动，空格或点击射击
   - 敌人：小队战斗机、大型巡洋、流星障碍，有入场与爆炸
   - HUD：分数、生命、连击、小地图雷达、准星
   - 暂停 / 重新开始 / 音效开关（Web Audio 简单合成即可，不强制外部音频文件）
   - 性能：目标 60fps（中端机），低性能自动降粒子与阴影；页面不可见时暂停渲染循环
   - 结束后成绩写入 localStorage 排行榜（Top 10），可在 Finder「太空档案」或游戏结算面板查看

10. **窗口系统（Window Manager）**
    - 每个 App 是可拖拽、可缩放（最小尺寸约束）、可最大化、可最小化、可关闭的窗口
    - 三色交通灯：红关 / 黄最小化 / 绿最大化或全屏（游戏绿键进全屏）
    - 焦点窗口置顶；标题栏双击最大化
    - z-index 与焦点环（强调色）明确可辨
    - 移动端：窗口默认最大化铺满，Dock 变为底部图标条，手势下滑关闭弹窗

## 设计要求

- **Liquid Glass**：半透明、背景模糊（`backdrop-filter`）、边缘高光、随壁纸染色。通透滑杆改变 `--glass-opacity` 与 `--glass-blur`
- **层次**：壁纸 < 桌面图标 < 窗口 < 弹出层 < Dock < 菜单栏（菜单栏视觉透明但命中在最前）
- **动效**：Framer Motion spring；禁止线性生硬闪烁。Dock 放大、窗口出现（scale 0.96 → 1 + fade）、弹窗从图标下方生长
- **浅色 / 深色都要好看**，对比度达标（文字不得落在过透的玻璃上发灰；Tinted 模式保证可读）
- **字体**：界面用系统 UI 字体栈（SF Pro 优先，回退 Inter / system-ui）；等宽用 SF Mono / JetBrains Mono / ui-monospace
- **圆角**：窗口 16–18px，弹窗 20–24px，Dock 容器 24–28px，图标 squircle
- **移动端适配**：< 768px 隐藏窗口拖拽，改为全屏卡片 + 底栏；游戏提供触控摇杆 + 射击键
- **无障碍**：焦点可见、弹窗 Esc 关闭、主题对比、减少动态效果跟随 `prefers-reduced-motion`

## 非目标（本期不做）

- 真实文件系统、真实联网扫描、真实 CPU 采集
- 多用户账号 / 后端同步
- 完整浏览器内核或任意第三方 App 运行时
- 安装到用户真实操作系统

## 成功标准

- 打开页面 3 秒内能感到「这是一台未来 Mac」而不是普通后台页
- Dock 五个 App 全部可打开、可关闭、可再打开，状态不串
- 主题、通透滑杆、强调色切换即时作用于菜单栏、Dock、窗口、弹窗
- 游戏可玩完整一局并记分，切后台暂停
- 1080p 桌面流畅；笔记本集成显卡可把游戏画质降到可玩
